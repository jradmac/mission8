@model Quadrent1

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="alert alert-success text-center shadow-lg p-4">
                <h2>You have added <strong>@Model.Task</strong> to your list!</h2>
            </div>
        </div>
    </div>
</div>